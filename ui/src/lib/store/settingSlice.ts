import { atom } from "jotai";

export const ATOM_scopedVars = atom(true);
export const ATOM_showAnnotations = atom(true);
export const ATOM_devMode = atom(false);
export const ATOM_copilotManualMode = atom(true);
export const ATOM_showLineNumbers = atom(false);
